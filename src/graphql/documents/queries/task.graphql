query GetTasksByUser($first: Int!, $after: String) {
  getTasksByUser(first: $first, after: $after) {
    edges {
      node {
        id
        title
        isCompleted
        createdAt
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}

# query GetTaskById(){
#   getTasksByUser {
#     id
#     title
#     isCompleted
#   }
# }
